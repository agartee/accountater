@model CsvImportSchemaInfo

@{
    ViewData["Title"] = "CSV Import Schema Details";
}

<h1>Edit CSV Import Schema</h1>

<form method="post" class="pure-form pure-form-aligned">
    
    <input type="hidden" asp-for="Id" />

    <div class="pure-control-group">
        <label asp-for="Name">Name</label>
        <input asp-for="Name" />
    </div>

    <div class="pure-controls control-panel">
        <button type="submit" class="pure-button pure-button-primary">Save</button>
        <a href="/csvimportschema" class="pure-button pure-button-secondary">Back</a>
    </div>

</form>

@if(Model != null)
{
    <h3>Danger Zone</h3>

    <div class="danger-zone">
        <form action="@Url.Action("Delete", "CsvImportSchema", new { id = Model.Id.Value })" method="post" 
              class="pure-form pure-form-aligned"
              onsubmit="return confirm('Are you sure you want to delete this CSV import schema?');">

            @Html.AntiForgeryToken()
            <input type="hidden" asp-for="Id" />

            <label>Delete this CSV import schema?</label>

            <button type="submit" class="pure-button button-danger delete-button">Delete</button>
            
        </form>
    </div>
}

@section Scripts {
    <script src="~/js/formModifiedChecks.js"></script>
}
